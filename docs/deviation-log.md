# Deviation Log — Upstream vs. Our Deployment

> **Upstream:** [Azure/Azure-Sentinel/DataConnectors/Syslog-VMSS-AMA](https://github.com/Azure/Azure-Sentinel/tree/master/DataConnectors/Syslog-VMSS-AMA)  
> **Tracking since:** 2026-06-29  
> **Purpose:** Every change from upstream is logged here so we can (a) justify our divergence and (b) identify what to PR back.

---

| # | Area | Upstream Behavior | Our Change | Reason | Upstream-PR-worthy? | Status |
|---|---|---|---|---|---|---|
| 1 | **OS Image** | Ubuntu 18.04-LTS (`UbuntuServer` / `18.04-LTS`) | Ubuntu 24.04-LTS Noble Gen2 (`Canonical` / `ubuntu-24_04-lts` / `server`, version `latest`) | 18.04 reached EOL April 2023. No security patches. Chose 24.04 Noble (newest LTS, support to 2029) over 22.04 to maximize runway — we won't deploy an EOL or near-EOL OS. | ✅ Yes | Implemented |
| 2 | **API Versions** | Very old API versions throughout (e.g., `Microsoft.Network 2019-09-01`, `Microsoft.Compute 2019-03-01`, `Microsoft.Insights 2014-04-01`, `DCR 2021-09-01-preview`) | Bump to current stable GA versions (2024-05-01 / 2024-07-01 range) | Old APIs may lack features, may be deprecated or removed. `2014-04-01` for autoscale is ancient. | ✅ Yes | Planned |
| 3 | **ARM → Bicep** | Single monolithic ARM JSON template | Modular Bicep with parameter file | Readability, type safety, linting, module reuse. | ⚠️ Partial (Bicep compiles to ARM; upstream may prefer ARM) | Planned |
| 4 | **TLS Syslog** | Not supported — plain-text 514 only | Add rsyslog TLS on port 6514 (GnuTLS driver), certs from Key Vault or cloud-init | Requirement: Syslog over TLS where possible. | ✅ Yes (optional feature) | Planned |
| 5 | **Cloud-init script** | Downloads `Forwarder_AMA_installer.py` from GitHub at deploy time | Inline custom cloud-init with rsyslog config + TLS setup; no runtime download from GitHub | Avoids external dependency at boot; enables TLS; more maintainable. | ⚠️ Partial (TLS part is new; removing wget is good practice) | Planned |
| 6 | **NSG — SSH rule** | Allows SSH (22) from `*` (any source) | Restrict SSH source to admin CIDR or remove entirely (use Azure Bastion / serial console) | Open SSH from internet is a security risk. | ✅ Yes | Planned |
| 7 | **NSG — Syslog rule** | Allows 514 from `*` | Scope source to known syslog-sender CIDRs; add 6514 rule for TLS | Principle of least privilege. | ✅ Yes | Planned |
| 8 | **Subnet resource typo** | Child subnet has `Microsoft.Networks` (extra 's') in NSG reference — likely causes deployment failure | Fix to `Microsoft.Network` | Bug in upstream template. | ✅ Yes (bug fix) | Planned |
| 9 | **VMSS SKU** | `Standard_F4s_v2` hardcoded | Parameterize SKU; default to `Standard_D2s_v5` or similar current-gen | Fsv2 is compute-optimized (expensive for syslog relay); Dv5 is more cost-effective. May also need to match region availability. | ⚠️ No (parameterize is best) | Planned |
| 10 | **AMA extension version** | `typeHandlerVersion: 1.22` | Use latest (`1.33`+ as of 2026) or rely on `autoUpgradeMinorVersion: true` | Upstream pins an old version. With auto-upgrade enabled the pin is less critical, but should reflect a modern baseline. | ✅ Yes | Planned |
| 11 | **Managed Identity name** | Hardcoded `managedIdentity` | Use naming convention: `{Base_Name}-uami` | Generic name clashes if multiple deployments exist in same RG. | ✅ Yes | Planned |
| 12 | **DCR API version** | `2021-09-01-preview` | Use GA version `2022-06-01` or later | Preview API may be removed. | ✅ Yes | Planned |
| 13 | **DCR name** | Hardcoded `default` | Use `{Base_Name}-dcr` or parameterized name | Generic name clashes. | ✅ Yes | Planned |
| 14 | **Autoscale cooldown** | 1 minute cooldown | Increase to 5 minutes | 1-minute cooldown causes aggressive scale thrashing. | ✅ Yes | Planned |
| 15 | **Load Balancer NAT pool** | Uses `inboundNatPools` (deprecated for Uniform VMSS in newer API versions) | Migrate to `inboundNatRules` with VMSS backend pool or remove SSH NAT entirely | `inboundNatPools` deprecated. | ✅ Yes | Planned |
| 16 | **Deployment identity (CI/CD)** | Manual "Deploy to Azure" button | GitHub Actions with OIDC federated credential (Workload Identity) | Automated, auditable, no stored secrets. | ❌ No (our CI choice) | Planned |
| 17 | **Workspace / Sentinel** | Expects existing workspace (params: subscription, RG, name, ID) | workspace.bicep creates workspace + Sentinel onboarding, or references existing by resourceId | Decision resolved: param `createWorkspace` (default true). | ❌ No | Implemented |
| 18 | **Sentinel API** | `Microsoft.OperationsManagement/solutions@2015-11-01-preview` is the only available version for SecurityInsights onboarding. No GA equivalent exists for this RP. | Use it as-is; note in code comments. | ⚠️ Known Azure limitation | Implemented |
| 20 | **Bicep linter — conditional PIP output** | N/A | BCP318 false positive: Bicep type-checker can't correlate `if (loadBalancerPublic)` resource guard with ternary guard in output. `#disable-next-line BCP318` applied; logic is correct. | Bicep compiler limitation, not an ARM defect. | ❌ No (Bicep toolchain) | Implemented |
| 18 | **KV secret format** | N/A (upstream has no KV) | syslog TLS cert material stored as Key Vault **Secrets** (PEM text), not KV Certificate objects | rsyslog requires PEM files; KV Certificates deliver PKCS#12 which requires conversion. Secrets give clean PEM retrieval. Manual rotation required (see docs/tls-setup.md). | ❌ No (our deployment pattern) | Implemented |
| 19 | **CI RBAC — User Access Administrator** | N/A (upstream has no CI) | GitHub Actions deploy identity (`uami-sc-syslog-deploy`) has User Access Administrator scoped to `rg-sc-syslog-collector-eastus2` only | `keyvault.bicep` creates a role assignment; Contributor alone does not include `roleAssignments/write`. UAA scoped to RG is the minimum required. | ❌ No (our CI/deployment pattern) | Implemented |
| 21 | **Cert delivery — single mechanism (cloud-init IMDS fetch)** | N/A (upstream has no KV or TLS) | `KeyVaultForLinux` VM extension **removed**. Sole cert delivery: `cloud-init`'s `fetch-kv-certs.sh` acquires an AAD token from IMDS (`https://vault.azure.net` audience) using the VMSS UAMI client ID, then calls KV REST API (`/secrets/{name}?api-version=7.4`) for each cert. KV name and UAMI client ID are baked into cloud-init at Bicep compile time via `loadTextContent + replace + base64` in `bicep/modules/vmss.bicep`. Writes: `syslog-ca-cert → ca.pem (0644)`, `syslog-server-cert → server.pem (0644)`, `syslog-server-key → server-key.pem (0600)`. | KV extension v2.0 writes secrets to opaque filenames incompatible with rsyslog's explicit cert path config. IMDS fetch gives deterministic filenames and eliminates a separate extension dependency. | ❌ No (our deployment pattern) | Implemented |
| 22 | **Deployment patterns & mTLS security guidance** | N/A — upstream has no deployment pattern documentation or security guidance beyond the basic ARM template README. | Comprehensive deployment pattern guide added at `docs/deployment-patterns.md`. Covers five escalating patterns (A: private/internal; B: hybrid/on-prem; C: public IP-allowlisted; D: public/open anti-pattern; E: public + mTLS), each with topology, NSG controls, TLS mode, threat model, residual risk, and concrete config deltas. Includes: threat-model primer distinguishing TLS anon vs. mTLS vs. NSG; defense-in-depth matrix; DoS/rate considerations; collector monitoring guidance; full mTLS certificate lifecycle (issuance, rotation, revocation, rsyslog/gtls CRL limitations); and Pattern E Bicep TODO list. Extends deviation-log item 4 (TLS syslog). Fully scrubbed of all private values; uses generic placeholders throughout. | Operator-facing security guidance is absent from upstream. mTLS (x509/name) is required for public internet deployments where sender IP allowlisting is insufficient. Documenting the full path enables deliberate future implementation. | ✅ Yes — vendor-neutral; upstream-PR-worthy as optional security guide | Documented |
| 23 | **Public-facing ALZ/Firewall hardening + cert rotation automation** | N/A — upstream has no guidance on ALZ hub-spoke topology, Azure Firewall integration, WAF applicability, or certificate rotation automation. | (a) **Pattern F** added to `docs/deployment-patterns.md §2.6`: ALZ hub-spoke topology with Azure Firewall Premium DNAT + Threat Intelligence + IDPS; explains why WAF (App Gateway / Front Door) does not apply to raw L4 TCP syslog; keep internal LB + remove public IP + front with Firewall; controls: Firewall DNAT, Threat Intel Deny, IDPS (L4/signature, NOT TLS inspection of syslog payload), DDoS Network Protection, NSG source allowlist, KV private endpoint, AMPLS, Defender for Servers, Bastion break-glass; honest cost/complexity callout; Pattern F is "if public ingress is mandatory" — default remains Pattern A/B. Combine Pattern F with Pattern E mTLS. Bring-your-own-hub note. Updated §3.1 defense-in-depth matrix, §5 comparison table, §6 decision guide. (b) **§4.5 cert rotation automation** added to `docs/deployment-patterns.md`: three documented-not-built approaches: Approach 1 (instance roll — immutable infra, via scheduled GitHub Actions); Approach 2 (on-box systemd timer re-fetch with checksum comparison, atomic swap, `reload-or-restart rsyslog`, `RandomizedDelaySec` stagger — concrete cloud-init YAML artifacts for `renew-certs.sh`, `.service`, `.timer`); Approach 3 (KV-side renewal: Option 3a KV Certificate auto-renew + Event Grid → Function bridge; Option 3b scheduled CI re-issue via GitHub Actions); expiry monitoring (Azure Monitor KV metric alert → Sentinel); mTLS rotation note (server cert vs. per-sender client cert vs. client CA). Cross-linked from `docs/tls-setup.md §3`. Decision tracked in a tracked enhancement (rotation) and a tracked enhancement (Pattern F). | Public-facing syslog ingestion needs concrete network-hardening guidance. WAF inapplicability is commonly misunderstood. Cert rotation automation closes a documented-but-not-built gap that would silently expire the TLS listener. Both are vendor-neutral, upstream-PR-worthy additions to the security guide. | ✅ Yes — extends deviation-log item 22; upstream-PR-worthy | Documented |
| 24 | **Egress via NAT Gateway** | N/A — upstream VMSS has no defined outbound path; instances rely on default outbound access (being retired by Azure Sept 2025) or implicit LB SNAT. | `network.bicep` provisions a dedicated NAT Gateway (`{base}-natgw` + standard public IP `{base}-natgw-pip`) and attaches it to the collector subnet. Adds `egressPublicIp` output so the deterministic outbound IP can be allowlisted by AMA/Key Vault/upstream services. | Azure is retiring default outbound internet access (Sept 30 2025); VMSS instances need an explicit, stable egress. NAT GW gives a single predictable SNAT IP, avoids SNAT port exhaustion under load, and is independent of the inbound LB. | Yes — modern outbound best practice | Implemented |
| 25 | **Cert material injected via @secure() params** | N/A (upstream has no KV/TLS). | `main.bicep` exposes `syslogCaCertPem` / `syslogServerCertPem` / `syslogServerKeyPem` as `@secure()` string params, passed to `keyvault.bicep`, which conditionally creates the matching KV secrets only when the param is non-empty. Enables a single `az deployment group create` to seed the TLS material at deploy time without a separate secret-upload step, while keeping PEMs out of source (params file is gitignored). | One-shot deploy that provisions infra and seeds certs together; keeps secret values transient (deployment-scoped secureString) and out of the repo. | No (deployment pattern) | Implemented |
| 26 | **rsyslog cert permissions (syslog group readable)** | N/A — upstream has no TLS, so no cert-permission handling. A naive port (dir `0700 root`, key `0600 root`) **breaks the TLS listener**. | cloud-init sets the cert directory to `0750 root:syslog` and all PEMs (`ca.pem`, `server.pem`, `server-key.pem`) to `0640 root:syslog` in all three places they are written (`fetch-kv-certs.sh`, runcmd dir-create, runcmd post-fetch enforcement). | **Key field finding.** rsyslog drops privileges to the `syslog` user (uid/gid 102) at runtime and the GnuTLS (`gtls`) driver reads the cert/key **per-connection, after the privilege drop**. Root-only perms cause `cannot read file server.pem: Permission denied` -> `gtlsInitSession failed` (rsyslog error **-2040**) -> the TLS handshake fails with `unexpected eof`. The `syslog` group exists by runcmd time because rsyslog is installed in the cloud-init packages stage. Verified end-to-end: handshake `Verify return code 0 (ok)`. | **Yes — highest-value upstream fix** (any TLS-syslog template hits this) | Implemented |
